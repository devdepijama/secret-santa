import { ParticipantSubscription } from "./participant-subscription";
import { Urn } from "./urn/urn";

export class Participant {

    writeDownSubscription() : ParticipantSubscription {
        return null;
    }

    takeOutSubscription(urn : Urn) : ParticipantSubscription {
        urn.
    }
}